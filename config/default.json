{

  "debug": {
    "desc": "Enable the debug messages",
    "type":"boolean",
    "value": true
  },

  "task": [
    {
      "files_max": {
        "desc": "Max allowed open files per task",
        "type": "integer",
        "value": 32
      }
    },
    {
      "stack_pages": {
        "desc": "Default stack size for the task in pages",
        "type": "integer",
        "value": 4
      }
    },
    {
      "lock_depth_max": {
        "desc": "Max allowed lock account per task",
        "type": "integer",
        "value": 8
      }
    }
  ],

  "core": [
    {
      "gpt": {
        "desc": "GPT partioning support",
        "type": "boolean",
        "value": true
      }
    }
  ],

  "fs": [
    {
      "fat32": {
        "desc": "FAT32 filesystem support",
        "type": "boolean",
        "value": true
      }
    }
  ]
}
